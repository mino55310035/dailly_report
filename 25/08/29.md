## 取り組んだ課題一覧
- react公式チュートリアル
- typescriptの基本
- react udemy
## わかったこと
- 静的型付け言語は実行前に型を宣言
- forEachメソッドでリストの各要素に対して処理を実行する
- 関数を定義するときはまず（）で囲んで引数に方を宣言。その後値を返す場合は方を宣言、返さない場合はvoidまたは省略
- && は左側がtruthyなら右側の値を返す
- || は左側の値がtruthyだったら左側の値を返す
## 次やること
- react udemy　続き
## 感じたこと
udemyがわかりやすい
## 学習時間
6.5時間
